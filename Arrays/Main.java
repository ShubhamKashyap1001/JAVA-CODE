public class Main {
    public static void main(String[] args) {
        //syntax
        // datatype[]nvariable_name = new datatype[size]
        int[] arr; // declaration of array, arr are store in the stack
        arr = new int[5] ;// initialisation: actually here object is created in heap
    }
}
